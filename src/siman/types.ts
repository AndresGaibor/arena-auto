export type SimanBlock = {
  index: number;
  name: string;
  type: string;
};

export type SimanSource = {
  source: string;
  length: number;
};

export type VbaMacro = {
  name: string;
  type: number;
};
